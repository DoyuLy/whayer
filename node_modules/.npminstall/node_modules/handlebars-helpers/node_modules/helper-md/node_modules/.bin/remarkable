#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../../../../../remarkable/1.6.2/remarkable/bin/remarkable.js" "$@"
  ret=$?
else 
  node  "$basedir/../../../../../remarkable/1.6.2/remarkable/bin/remarkable.js" "$@"
  ret=$?
fi
exit $ret
